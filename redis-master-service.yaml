apiVersion: v1
kind: Service
metadata:
  name: lifesense-qa2-redis-master
  namespace: base
  labels:
    app: redis
    role: master
    tier: backend
spec:
  ports:
  - nodePort: 31764
    port: 6379
    protocol: TCP
    targetPort: 6379

  selector:
    app: redis
    role: master
    tier: backend
